{
    "configurations": [
        {
            "name": "Debug Build",
            "program": "${workspaceFolder}/build/build.js",
            "request": "launch",
            "type": "node"
        }
    ]
}